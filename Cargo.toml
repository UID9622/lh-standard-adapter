[package]
name = "lh-standard-adapter"
version = "1.0.0"
edition = "2021"
description = "LongHun Standard Adapter — DNA traceability and audit metadata wrapper"
license = "CC-BY-NC-SA-4.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
